import pepe.*

test "sueldo neto de pepe cadete" {
	pepe.categoria(cadete)
	assert.equals(20000, pepe.sueldoNeto())
}

test "sueldo neto de pepe gerente" {
	pepe.categoria(gerente)
	assert.equals(15000, pepe.sueldoNeto())
}
